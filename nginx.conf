upstream app {
    server app-1:8080;
    server app-2:8080;
    server app-3:8080;
}

server {
    listen 80;
    client_max_body_size 105M;

    location / {
        proxy_pass http://app;
        add_header X-Served-By $upstream_addr;
    }
}
