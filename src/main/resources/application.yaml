spring:
  application:
    name: hobby-adventure
  profiles:
    active: local

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.jian.hobbyadventure
  configuration:
    map-underscore-to-camel-case: true

server:
   port: 8080
